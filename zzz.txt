Gates:

Get % shown on gates - implement for composite gates and not working on scaled params??
Gates should display default params


Add filter Cache





NOT gates
AND gates
OR gates

context menu on gate sidebar + renaming + deleting
name gates
Delete gates


Meta:

Metadata
Metadata Groups
Metadata-specific Gating
Set Metadata from FCS or Omiq
Sample-specific gating

Paired samples (FMX FS)


Omiq Integration:

Set gates from omiq gating
Save gates to omiq gating

Set Scaling from omiq Scaling
Send Scaling to omiq Scaling

Set Comp from omiq Comp


Nice to have:
NAND gates
NOR gates
Contourplot
Histogram


