Gates:

gateresolver should not be a part of the store and should not be a store.
the gatestore has a method, - its current set_current_file method except it RETURNS a gatestore.
This method watches the 3 gate_maps
The gate_layer holds its own resolver in a use_memo and queries it for plots
On plot updates the resolver is a middleman that knows the origin map of the gate
This does mean the whole resolver is remade on each gate move etc but its relatively quick


Allow choosing other gates at the same level for AND and OR
stats for boolean gates??

Add filter Cache

rename gates
Show names on gates


Meta:

Metadata
Metadata Groups
Set Metadata from FCS or Omiq


Paired samples (FMX FS)


Omiq Integration:

Set gates from omiq gating
Save gates to omiq gating

Set Scaling from omiq Scaling
Send Scaling to omiq Scaling

Set Comp from omiq Comp


Nice to have:
Contourplot
Histogram
label positions

